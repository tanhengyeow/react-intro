var React = require('react');
var ReactDOM = require('react-dom'); //render react component with the dom

require('./index.css') //all css in index.css included inside the application when webpack bundles up everything

